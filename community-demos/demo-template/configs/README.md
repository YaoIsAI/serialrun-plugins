# Configs / 配置

把你的 SerialRUN MCP 配置文件放在这里。

Put your SerialRUN MCP config files here.

**⚠️ 重要 / IMPORTANT**: 必须脱敏！删除任何 IP、密钥、个人路径。
MUST sanitize! Remove any IP, keys, personal paths.

**文件 / Files**:
- `claude-desktop-config.json` - Claude Desktop MCP 配置
- `serialrun-config.toml` - SerialRUN 配置文件
